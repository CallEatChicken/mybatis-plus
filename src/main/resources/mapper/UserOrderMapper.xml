<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.plus.mapper.UserOrderMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.mybatis.plus.bean.UserOrder">
        <id column="id" property="id" />
        <result column="userId" property="userId" />
        <result column="shopName" property="shopName" />
        <result column="shopId" property="shopId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, userId, shopName, shopId
    </sql>
    
    
    <select id="selectListOrder" resultType="com.mybatis.plus.bean.UserOrder">
		SELECT * FROM user_order
		<!-- 判断 wrapper 是否为空 emptyOfWhere -->
		<where>
			${ew.sqlSegment}
		</where>
	</select>

</mapper>
